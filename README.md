# javscript-runjs

A simple deno runtime for running javascript files using rust by using the deno core library and tokio as the evnet loop.

## Usage

run in the root project directory
```bash
cargo run
```
